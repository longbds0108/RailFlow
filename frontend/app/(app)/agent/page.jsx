import EnvelopeComingSoon from "../../../components/EnvelopeComingSoon";

export default function AgentPage() {
  return (
    <EnvelopeComingSoon
      eyebrow="Arc Testnet · Railflow Protocol"
      title="Agent"
      description="Hand a sleeve to the agent under a signed mandate — it can act inside your limits, never outside them."
      note="The agent mandate flow is being wired up. Check back soon."
    />
  );
}
